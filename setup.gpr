project Setup is

   for Source_Dirs use (".", "src");
   for Main use ("setup_driver.adb");

   package Compiler is
      for Switches ("ada") use ("-g", "-O2");
   end Compiler;

   for Object_Dir use "./obj";
   for Exec_Dir use "./bin";

end Setup;

